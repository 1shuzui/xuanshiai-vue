# 待完成事项

## 媒体上传后再发布动态

状态：已接入（前端）。

发布页与纸飞机页已在选择媒体后调用 `uploadCommunityMedia`，发布/放飞仅传 `imageMediaIds` / `videoMediaId`；取消或卸载时对未绑定的 ready 媒体调用 `deleteCommunityMedia`。真实环境仍依赖生产 HTTPS 域名与小程序合法域名配置；局域网开发地址不能作为生产域名。

## 微信小程序端侧回归（阻塞）
状态：阻塞。

本次在 HBuilderX 启动 `mp-weixin` 运行时，编译尚未开始即被“当前项目未关联 uniCloud 服务空间”终止。因此，本次不将未完成的 `mp-weixin` 编译或微信开发者工具回归声称为已通过。

下一步：在可用的开发环境关联正确的 uniCloud 服务空间，或使用已关联的环境重试。请不要通过修改 `manifest.json`、`pages.json` 或 uniCloud 配置来绕过这个验证前置；关联后需重新执行编译与关键社区流程的端侧回归。

## 微信小程序包体质量治理（计划）

状态：已登记，后期治理；首次正式上传或提审前必须完成。

当前 `mp-weixin` 产物结构检查通过，但质量审计发现：主包 4,014,431 字节（3.828 MiB），没有分包，生成 `app.json` 未开启 `lazyCodeLoading: requiredComponents`，5 张 JPG 超过 200 KiB，另有 32 个 Iconfont 预览残留文件共 487,973 字节。主包超过 2 MiB 是上传阻塞，其余问题会持续放大包体和首屏成本。

本轮不直接修改受保护的 `manifest.json`、`pages.json` 或二进制资源。后续按 [`WECHAT_PACKAGE_GOVERNANCE_PLAN.md`](../WECHAT_PACKAGE_GOVERNANCE_PLAN.md) 分阶段实施：建立项目内门禁、开启组件按需注入、清理/压缩资源、迁移三个业务分包，再通过 HBuilderX 和微信开发者工具验收。
