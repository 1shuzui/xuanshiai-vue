# 宣誓爱项目 — AGENTS.md

> **文档版本：** 2.1.0  
> **生效日期：** 2026-07-22  
> **适用对象：** 所有 AI 助手、开发者、协作者  
> **项目主线：** `./`（UniApp Only）  
> **定版 PRD：** 已登记（见第 2 节；完整规则以仓库根目录 `AGENTS.md` 为准）

本文件是工程内执行规则副本。与仓库根目录 `../AGENTS.md` 冲突时，以根目录为准并应同步本文件。用户对本次任务的明确授权可以允许修改被点名的文档，但不能默认解除其他安全、架构和产品约束。

## 1. 开发主线：UniApp Only

### 必须遵守

- 所有生产代码、页面、组件、样式、接口和联调都在 `./` 内完成。
- 微信小程序是第一验收端，H5 只用于快速预览和联调。
- 页面使用 UniApp / Vue 3 / UTS 能力，优先保证跨端安全。
- 新页面放在 `pages/`，新组件放在 `components/`，工具放在 `utils/`，接口与假数据放在既有 `api/` / `mock/` 约定内。

### 绝不做

- 不新建或迭代仓库根目录 `demo*.html`，不回到 HTML 高保真迁移路线。
- 不把已下线的 HTML 演示目录当生产入口；视觉以 `DESIGN.md`、`uni.scss` 与最终版分页面截图为准。
- 不恢复、依赖或维护历史 XMind 源文件与旧路径；需求以已登记的定版 PRD 为准。
- 不把普通 Vue 网站的 DOM / 浏览器 API 当作 UniApp 默认实现。

## 2. 需求与文档优先级

### 2.1 定版 PRD（已登记）

| 层级 | 路径（相对本工程） | 用途 |
|---|---|---|
| **决策层（最高）** | `../最终版的文字需求/定版决策收口记录.md` | 本期/二期、认证、喜欢/收藏、次数、付费边界 |
| **文字层** | `../最终版的文字需求/` | 功能文字；与决策冲突时以决策为准 |
| **页面与截图层** | `../最终版分页面拆分的相关需求/` | 分页面结构与截图 |

### 2.2 冲突处理顺序

1. 用户本次明确授权。
2. 定版 PRD 决策层。
3. 定版 PRD 文字层与分页面拆分层。
4. 本文件 / 根目录 `AGENTS.md` 的硬约束。
5. `PRODUCT.md`（须与决策层收敛；未回写处以决策层为准）。
6. `DESIGN.md`。
7. 当前 `./` 代码与 Mock。
8. `docs/*` 工程文档与过渡/对照材料。

页面 md 中的导图残留句（VIP 锁定、爆灯存疑、会员开通等）不得直接当实现依据。重大变更先更新定版 PRD 与权威文档，再改代码。

## 3. 产品硬约束

宣誓爱是认真婚恋产品，不是泛娱乐社交。

### 核心体验

- 首页围绕 **故事 → 信任 → 行动**：先读生活和关系期待，再看认证与档案，再做收藏、喜欢或申请认识。
- 五个一级 Tab 固定为：首页、社区、牵线、消息、我的。
- **双重认证（本期门槛）**：实名（公安人脸）+ 单身承诺书（电子签名）；状态必须真实展示。
- **收藏** 仅自己可见；**喜欢** 为对对方可见的轻意向；**申请认识** 是聊天前置门槛；双方同意后才开放聊天。
- 申请认识每日免费 3 次；用尽后积分加次，不弹 VIP、不现金买次。
- AI 合拍度只提供资料交集、关系期待和破冰建议，不承诺关系结果。

### 禁止功能与模式

- 不做 Tinder 式左右滑动匹配、颜值榜、娱乐化陪聊或低俗内容；左滑仅纯浏览，删除「不感兴趣」负反馈。
- 本期隐藏爆灯；会员订阅购买与 VIP 开通引导为二期。
- 不做双方同意前的即时聊天、联系方式交换、自动代发或自动同意。
- 不降低认证门槛，不允许他人照片、虚假学历、已婚标单身等行为。
- 不使用模糊价格、虚假倒计时、默认连续包月或隐藏限制的付费逻辑。
- 不以“马上脱单”“命中注定”“最后机会”等文案制造焦虑或保证结果。

## 4. 视觉系统硬约束

### Token 来源

`uni.scss` 是全局 Token 的代码来源，`DESIGN.md` 是规则与解释来源。新代码必须优先复用：

- 背景：`--canvas`、`--paper`、`--surface`、`--warm`
- 文本：`--ink`、`--ink2`、`--muted`
- 边界：`--line`
- 强调：`--accent`、`--accent2`、`--soft`
- 功能：`--brown`、`--navy`、`--sage`、`--sage-soft`
- 字体：`--serif`、`--sans`

禁止在页面中散落新色值、`#000`、`#fff` 或未经评审的颜色变量。底层 `--surface` 可以是中性白，但业务代码只能通过 Token 使用。

### 视觉语言

- 冷瓷白底、低饱和青瓷绿强调、深青灰正文、宋体叙事、无衬线系统信息。
- 8px 为主要间距网格，内容左右内边距通常为 16px。
- 普通卡片圆角遵循 4px / 8px / 12px / 999px 等级；人物媒体保留既有非对称圆角作为识别，不把大圆角复制到所有元素。
- 普通卡片使用浅边界或轻阴影，不叠加“边框 + 大阴影”装饰。
- 阴影使用 rgba 软阴影（`var(--shadow-sm|md|lg)`），不使用纯黑阴影、渐变文字、装饰性玻璃拟态或重复网格背景。运行时 Token 为 hex/rgba，全局注入 `App.uvue` 的 `page` 与 `uni.scss`，禁止业务代码写 `oklch()`。
- 动效主要为 0.18s—0.22s ease-out；按压态可以 `scale(0.98)`，不能让内容依赖动画出现。

## 5. 组件与页面规则

### 复用优先

新增 UI 前先检查 `Xsa*` 组件：

`XsaButton`、`XsaCard`、`XsaInput`、`XsaTag`、`XsaTabs`、`XsaSheet`、`XsaModal`、`XsaToast`、`XsaEmpty`、`XsaUserCard`、`XsaDynamicCard`、`XsaMessageItem`、`XsaPhotoGrid`。

只有现有组件无法表达需求时，才新增可复用组件或变体；不得为单个 HTML 演示造组件。

### 移动端适配

- 覆盖最小 320px、标准 375 / 390px、最大 428px。
- 使用 flex、scroll-view、rpx / 安全的 px 和安全区域；不依赖浏览器 DOM。
- 固定头部、底部 Tab、底部操作栏必须为滚动内容预留空间。
- 可点击目标尽量达到 44px；图标按钮必须有可理解的标签。
- 图片优先 WebP，必要时 JPEG 降级；列表图片懒加载，大资源不得无规划进入主包。

### 状态与隐私

页面必须考虑加载、空、错误、无权限、成功、重复点击和网络失败状态。举报、安全提示、资料公开范围和数据删除路径不能被隐藏。敏感对话和联系方式不应被无必要记录或展示。

## 6. 受保护文件与操作权限

### 未经明确确认不得修改

- `manifest.json`
- `pages.json`
- `uniCloud-aliyun/`
- 依赖的大版本、已有依赖删除、发布 Tag、他人分支
- 其他未被用户点名的核心配置

用户明确点名修改本文件、`PRODUCT.md`、`DESIGN.md` 或 `CLAUDE.md` 时，视为对这些文档的本次授权；改动仍要保持相互一致并通过检查。

### 允许操作

- 在 `pages/` 创建页面、在 `components/` 创建复用组件。
- 修改页面和组件的 `.uvue` 逻辑与样式。
- 在既有 `api/`、`mock/` 约定内扩展接口和假数据。
- 在 `utils/` 添加工具函数。
- 更新项目说明、设计文档、产品文档和本规则文件（需有明确任务依据）。

## 7. 技术与验证流程

开始工作前：

1. 阅读本文件、`PRODUCT.md`、`DESIGN.md` 和相关页面 / 组件。
2. 若 `../graphify-out/graph.json` 存在，先运行 `graphify query` 缩小代码范围，再回源文件核对。
3. 检查工作区，保留用户已有改动。
4. 修改前确认没有违反产品或设计边界。

完成工作前先执行当前可用的结构检查：

```bash
cd .
node tests/test-mock-system.js
```

截至 2026-07-22，npm CLI 会先因根目录式工程寻找 `src/manifest.json`；手动指定根目录后仍会在解析 `App.uvue` 时失败。不要把 `npm run build:h5` / `npm run build:mp-weixin` 写成已通过门禁，也不要为绕过问题移动受保护配置或批量改写 `.uvue`。端侧编译优先使用 HBuilderX，并如实记录实际结果。

并检查：

- H5 结构和交互可用。
- 微信小程序关键路径可回归，不能只凭 H5 结果宣称完成。
- 320—428px 无横向溢出，固定栏不遮挡内容。
- 认证、申请认识、双方同意、聊天门槛和安全提示逻辑正确。
- 文档标题、Token、路由、产品规则没有互相矛盾。
- `git diff --check` 通过；无法运行的检查必须如实说明。

修改代码或重要项目文档后，从工作区根目录（当前工程的父级）运行 `graphify update .`，保持知识图谱与实际内容同步。

## 8. 对话协作规则

### 请求与规则冲突时

先指出冲突，再给出符合规范的替代方案；如果需求确实需要换色、改产品边界、改核心路由或变更数据结构，应先说明需更新的权威文档与风险，等待明确确认。

### 请求不明确时

必须补足至少两个关键维度：

- 目标 UniApp 页面 / 组件。
- 这是主行动、次行动还是文字操作。
- 是否已有 Xsa 组件可复用。
- 对应哪条产品流程、认证或安全规则。

### 不得做出的错误建议

- 不建议用纯黑 / 纯白字面色值临时顶上。
- 不建议跳过认证、先开放聊天、弱化举报或暂不考虑安全。
- 不建议先做网站版绕过 UniApp 限制。
- 不建议用付费绕过双方同意或透明化规则。

## 9. Checklist

### 新增页面 / 组件

- [ ] 代码位于 `./`。
- [ ] 符合定版 PRD 或已声明临时依据。
- [ ] 使用 `DESIGN.md` / `uni.scss` Token。
- [ ] 已检查并优先复用 `Xsa*` 组件。
- [ ] 覆盖加载、空、错误、无权限和成功状态。
- [ ] 考虑小程序兼容、可访问性、图片性能和安全提示。
- [ ] 关键路径在 H5 与微信小程序验证。

### 修改已有功能

- [ ] 没有破坏五 Tab、申请认识或聊天门槛。
- [ ] 没有引入未评审颜色、字体、圆角或付费逻辑。
- [ ] 没有影响其他页面而未说明。
- [ ] 需要时已同步 PRODUCT / DESIGN / 项目文档。

### 提交前

- [ ] 运行可用的构建与静态检查。
- [ ] 运行 `graphify update .`。
- [ ] 运行 `git diff --check`。
- [ ] 汇报验证通过项、未验证项和已知限制。

## 10. 文档维护

重大变更的同步要求：

- 产品定位、范围、流程、商业化、安全 → 更新 `PRODUCT.md`。
- Token、字体、组件、视觉语言 → 更新 `DESIGN.md` 与必要的 `uni.scss`。
- 技术架构、命令、目录和跨端说明 → 更新 `CLAUDE.md` / `README.md`。
- 协作硬约束、需求源或权限流程 → 更新 `AGENTS.md`。

文档版本不能脱离实际代码。若发现代码与文档长期漂移，先记录差异、判断权威来源，再一次性收敛，不用口头约定掩盖冲突。

## graphify

工作区存在 `../graphify-out/` 知识图谱。代码库问题先查询图谱；查询结果不足时再扩大源文件范围。不要把图谱推断当作事实，必须回到 `source_location` 对应文件核对。修改代码或重要项目文档后执行 `graphify update .`。


