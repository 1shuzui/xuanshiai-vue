<template>
	<view class="page">
		<!-- 导航栏 -->
		<view class="nav-bar">
			<text class="nav-title">我的</text>
			<view class="nav-right">
				<view class="nav-icon-btn" @tap="onMessages">
					<text class="nav-icon">🔔</text>
				</view>
				<view class="nav-icon-btn" @tap="onSettings">
					<text class="nav-icon">⚙</text>
				</view>
			</view>
		</view>

		<!-- 滚动内容区 -->
		<scroll-view scroll-y class="content-scroll" enhanced show-scrollbar="false">
			<!-- 个人资料卡 -->
			<view class="profile-card" @tap="onViewMyProfile">
				<view class="profile-top">
					<view class="avatar-wrap">
						<image class="avatar-img" src="/static/portraits/profile-woman-main.jpg" mode="aspectFill"></image>
					</view>
					<text class="nickname">林悦</text>
					<text class="user-id">ID: 20240818</text>
					<view class="edit-btn" @tap="onEditProfile">
						<text class="edit-icon">✎</text>
						<text class="edit-text">编辑资料</text>
					</view>
				</view>
			</view>

			<!-- VIP会员卡 -->
			<view class="vip-card" @tap="onOpenVIP">
				<view class="vip-row">
					<view class="vip-left">
						<text class="vip-crown">👑</text>
						<text class="vip-title">开通VIP会员</text>
					</view>
					<text class="vip-arrow">→</text>
				</view>
			</view>

			<!-- 功能入口网格 -->
			<view class="features-card">
				<!-- 第一行 -->
				<view class="feat-row">
					<view class="feat-cell" v-for="feat in featureRow1" :key="feat.label" @tap="feat.action">
						<view class="feat-icon-circle" :style="{ backgroundColor: feat.bgColor }">
							<text class="feat-icon">{{ feat.icon }}</text>
						</view>
						<text class="feat-label">{{ feat.label }}</text>
					</view>
				</view>
				<view class="divider"></view>
				<!-- 第二行 -->
				<view class="feat-row">
					<view class="feat-cell" v-for="feat in featureRow2" :key="feat.label" @tap="feat.action">
						<view class="feat-icon-circle" :style="{ backgroundColor: feat.bgColor }">
							<text class="feat-icon">{{ feat.icon }}</text>
						</view>
						<text class="feat-label">{{ feat.label }}</text>
					</view>
				</view>
			</view>

			<!-- 设置菜单 -->
			<view class="settings-card">
				<view class="setting-row" v-for="item in settingItems" :key="item.label" @tap="item.action">
					<view class="setting-left">
						<text class="setting-icon">{{ item.icon }}</text>
						<text class="setting-label">{{ item.label }}</text>
					</view>
					<text class="setting-arrow">›</text>
				</view>
			</view>

			<!-- 底部留白 -->
			<view class="scroll-bottom-spacer"></view>
		</scroll-view>
	</view>
</template>

<script setup lang="uts">
const onMessages = () => {
	uni.switchTab({ url: '/pages/index/index' })
}
const onSettings = () => {
	uni.navigateTo({ url: '/pages/profile/settings' })
}
const onViewMyProfile = () => {
	uni.navigateTo({ url: '/pages/user/edit' })
}
const onEditProfile = () => {
	uni.navigateTo({ url: '/pages/user/edit' })
}
const onOpenVIP = () => {
	uni.navigateTo({ url: '/pages/profile/vip' })
}
const onMyMatch = () => {
	uni.showToast({ title: '我的牵线', icon: 'none' })
}
const onMyFav = () => {
	uni.showToast({ title: '我的收藏', icon: 'none' })
}
const onWhoViewed = () => {
	uni.showToast({ title: '谁看过我', icon: 'none' })
}
const onHistory = () => {
	uni.showToast({ title: '浏览记录', icon: 'none' })
}
const onMyMoments = () => {
	uni.showToast({ title: '我的动态', icon: 'none' })
}
const onVerify = () => {
	uni.navigateTo({ url: '/pages/profile/certification' })
}
const onMyGifts = () => {
	uni.showToast({ title: '我的礼物', icon: 'none' })
}
const onHelp = () => {
	uni.showToast({ title: '帮助中心', icon: 'none' })
}
const onAccountSecurity = () => {
	uni.showToast({ title: '账号安全', icon: 'none' })
}
const onNotifySettings = () => {
	uni.showToast({ title: '消息通知', icon: 'none' })
}
const onPrivacySettings = () => {
	uni.showToast({ title: '隐私设置', icon: 'none' })
}
const onAbout = () => {
	uni.showToast({ title: '关于我们', icon: 'none' })
}

const featureRow1 = [
	{ icon: '♥', label: '我的牵线', bgColor: '#FEF0F0', action: onMyMatch },
	{ icon: '★', label: '我的收藏', bgColor: '#FEF9F0', action: onMyFav },
	{ icon: '👁', label: '谁看过我', bgColor: '#F0F8F4', action: onWhoViewed },
	{ icon: '🕐', label: '浏览记录', bgColor: '#F0F4FA', action: onHistory }
]
const featureRow2 = [
	{ icon: '👥', label: '我的动态', bgColor: '#F5F0FF', action: onMyMoments },
	{ icon: '🛡', label: '实名认证', bgColor: '#F0FCF6', action: onVerify },
	{ icon: '🎁', label: '我的礼物', bgColor: '#FFFCF0', action: onMyGifts },
	{ icon: '💬', label: '帮助中心', bgColor: '#F5F5F8', action: onHelp }
]
const settingItems = [
	{ icon: '👤', label: '个人资料', action: onEditProfile },
	{ icon: '🔒', label: '账号安全', action: onAccountSecurity },
	{ icon: '🔔', label: '消息通知', action: onNotifySettings },
	{ icon: '🛡', label: '隐私设置', action: onPrivacySettings },
	{ icon: 'ℹ', label: '关于我们', action: onAbout }
]
</script>

<style scoped>
/* ==================== 页面容器 ==================== */
.page {
	position: relative;
	width: 100%;
	max-width: 750rpx;
	min-height: 100vh;
	margin: 0 auto;
	background-color: var(--canvas);
	display: flex;
	flex-direction: column;
}

/* ==================== 状态栏占位 ==================== */
.status-bar-placeholder {
	height: 88rpx;
}

/* ==================== 导航栏 ==================== */
.nav-bar {
	display: flex;
	flex-direction: row;
	align-items: center;
	justify-content: center;
	padding: 32rpx 40rpx 24rpx 40rpx;
	position: relative;
	background-color: var(--canvas);
}

.nav-title {
	font-family: var(--serif);
	font-size: 36rpx;
	font-weight: 700;
	color: var(--ink);
}

.nav-right {
	position: absolute;
	right: 40rpx;
	top: 32rpx;
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 16rpx;
}

.nav-icon-btn {
	width: 72rpx;
	height: 72rpx;
	border-radius: 999px;
	background-color: var(--warm);
	display: flex;
	align-items: center;
	justify-content: center;
}

.nav-icon {
	font-size: 32rpx;
}

/* ==================== 滚动内容区 ==================== */
.content-scroll {
	flex: 1;
	padding: 0 32rpx;
}

/* ==================== 个人资料卡 ==================== */
.profile-card {
	display: flex;
	flex-direction: column;
	background-color: var(--surface);
	border-radius: 16px;
	overflow: hidden;
	box-shadow: var(--shadow-sm);
	margin-bottom: 24rpx;
}

.profile-top {
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 16rpx;
	padding: 64rpx 40rpx 40rpx 40rpx;
}

.avatar-wrap {
	width: 160rpx;
	height: 160rpx;
	border-radius: 80rpx;
	overflow: hidden;
	background-color: var(--warm);
}

.avatar-img {
	width: 100%;
	height: 100%;
}

.nickname {
	font-family: var(--serif);
	font-size: 40rpx;
	font-weight: 700;
	color: var(--ink);
}

.user-id {
	font-size: 24rpx;
	color: var(--muted);
}

.edit-btn {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 8rpx;
	padding: 16rpx 40rpx;
	border-radius: 999px;
	background-color: var(--accent-bg);
	margin-top: 8rpx;
}

.edit-icon {
	font-size: 26rpx;
	color: var(--accent2);
}

.edit-text {
	font-size: 26rpx;
	font-weight: 600;
	color: var(--accent2);
}

/* ==================== VIP会员卡 ==================== */
.vip-card {
	margin-bottom: 24rpx;
	height: 144rpx;
	border-radius: 16px;
	background: linear-gradient(135deg, #2D4A3E, #3D5640, #C9A96E);
	display: flex;
	align-items: center;
	padding: 0 40rpx;
}

.vip-row {
	width: 100%;
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
}

.vip-left {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 20rpx;
}

.vip-crown {
	font-size: 56rpx;
}

.vip-title {
	font-size: 30rpx;
	font-weight: 700;
	color: var(--text-invert);
}

.vip-arrow {
	font-size: 32rpx;
	font-weight: 600;
	color: #C9A96E;
}

/* ==================== 功能入口网格 ==================== */
.features-card {
	display: flex;
	flex-direction: column;
	background-color: var(--surface);
	border-radius: 16px;
	overflow: hidden;
	box-shadow: var(--shadow-sm);
	margin-bottom: 24rpx;
}

.feat-row {
	display: flex;
	flex-direction: row;
	padding: 32rpx 24rpx;
}

.feat-cell {
	flex: 1;
	display: flex;
	flex-direction: column;
	align-items: center;
	gap: 16rpx;
}

.feat-icon-circle {
	width: 96rpx;
	height: 96rpx;
	border-radius: 48rpx;
	display: flex;
	align-items: center;
	justify-content: center;
}

.feat-icon {
	font-size: 44rpx;
}

.feat-label {
	font-size: 24rpx;
	font-weight: 500;
	color: var(--ink2);
}

.divider {
	width: 100%;
	height: 2rpx;
	background-color: var(--line);
}

/* ==================== 设置菜单 ==================== */
.settings-card {
	display: flex;
	flex-direction: column;
	background-color: var(--surface);
	border-radius: 16px;
	overflow: hidden;
	box-shadow: var(--shadow-sm);
	margin-bottom: 24rpx;
}

.setting-row {
	display: flex;
	flex-direction: row;
	justify-content: space-between;
	align-items: center;
	padding: 32rpx 40rpx;
	border-bottom: 2rpx solid var(--line);
}

.setting-row:last-child {
	border-bottom: none;
}

.setting-left {
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 24rpx;
}

.setting-icon {
	font-size: 36rpx;
}

.setting-label {
	font-size: 30rpx;
	font-weight: 500;
	color: var(--ink);
}

.setting-arrow {
	font-size: 40rpx;
	font-weight: 400;
	color: var(--muted);
}

/* 底部留白 */
.scroll-bottom-spacer {
	height: 120rpx;
}
</style>
